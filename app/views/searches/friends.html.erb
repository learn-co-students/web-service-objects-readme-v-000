<ul>
  <% if @friends.count > 1 %>
    <% @friends.each do |friend| %>
      <li><%= "#{friend['firstName']} #{friend['lastName']}" %></li>
    <% end %>
  <% else %>
    <p>You have no friends.</p>
  <% end %>
</ul>
