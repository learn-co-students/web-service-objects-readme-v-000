<ul>
  <% if @friends.size > 0 %>
    <% @friends.each do |friend| %>
      <li><%= "#{friend['firstName']} #{friend['lastName']}" %></li>
    <% end %>
  <% else %>
    <h5>You don't have any foursqaure friends yet</h5>
  <% end %>
</ul>
